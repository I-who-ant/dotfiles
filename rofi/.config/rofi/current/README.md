# Current rofi entrypoints
#
# Hyprland points to this directory instead of directly referencing one of the
# upstream `type-*` theme folders.
#
# Change the active launcher or powermenu look here first, then keep the theme
# asset folders as implementation details.
