## Current applet theme

current_theme_dir="$HOME/.config/rofi/applets/type-1"
current_theme_style='style-1.rasi'

# Keep these legacy variable names because the upstream scripts expect them.
type="$current_theme_dir"
style="$current_theme_style"
